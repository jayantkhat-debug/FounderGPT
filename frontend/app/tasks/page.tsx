import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function TasksPage() {
  return (
    <PlaceholderPage
      title="Tasks"
      description="Manage weekly founder focus, execution tasks, and critical next moves."
      ctaHref="/dashboard"
      ctaLabel="Back to dashboard"
    />
  );
}
