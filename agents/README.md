# FounderGPT X Agents

Runtime agent definitions live in `backend/app/agents/registry.py`.

This folder is reserved for product-facing agent documentation, evaluation rubrics, and future prompt versioning. Agent prompts should remain role-specific, challenge-oriented, and grounded in project memory.
